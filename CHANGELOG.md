# 更新日志

## 4.1.3

- 修复：配置向导无法清除访问口令（新增「访问控制」开关，显式关闭鉴权进入开放模式）。

## 4.1.2

- 修复：鉴权模式下登录成功后被弹回登录页、无法进入应用（`/api/auth/me` 未校验会话）。

## 4.1.1

- 播放栏常驻显示：无试听时也显示底部播放栏（占位态）。
- 优化：「检查更新」增加结果提示（发现新版本 / 已是最新版本）。

## 4.1.0

- UI 重新设计：现代化深色主题（Apple Music 风格）、侧边栏导航、毛玻璃底部播放条。
- 修复：酷狗歌单广场歌曲数与播放量不显示。

## 4.0.1

- 新增：试听音量调节。
- 优化：试听改为速度优先（从最低音质 128k 起尝试，文件更小、加载更快）。

## 4.0.0

- 新增：全局试听播放器（底部播放条：封面 / 歌名 / 歌手、播放暂停、可拖动进度条与时长）。
- 新增：排行榜、歌单详情歌曲支持试听。
- 新增：试听歌词同步显示（解析 LRC，按播放进度高亮当前行）。

## 3.0.1

- 优化：歌单广场与歌单详情改为翻页（歌单歌曲 100 首/页，100 首以内一页全显示）。
- 修复：歌单「全部下载」入队整个歌单（此前仅入队当前页 30 首）。
- 新增：歌单多选下载（勾选多首选择性下载），与「全部下载」共存。
- 移除：歌单广场咪咕平台（接口失效）。

## 3.0.0

- 工程质量：TypeScript 类型检查 52 处错误清零、ESLint 24 处问题清零，新增 typecheck / lint 门禁。
- 安全：歌词抓取接入 safeFetch，SSRF 加固覆盖音源脚本、歌单解析、封面、歌词全部出口。
- 修复：关键路径静默失败改为可见日志（本地预检、多版本入待确认、统计/对账/音源加载等）。
- 修复：消除 file_path 可空类型等历史类型隐患，音源脚本逃逸 payload 拦截、熔断与自愈异步化等健壮性完善。

## 2.0.5

- 安全：SSRF 加固，新增 safeFetch（IP 直连 + 原域名 SNI/Host，逐跳重新校验），修复 DNS rebinding；音源脚本抓取、歌单解析、封面下载改走安全抓取。
- 修复：下载目录扫描缓存于下载落盘后主动失效；下载状态自愈异步化（内存集合判断 + 分批让出事件循环）。
- 工程：新增 typecheck / lint 脚本与 ESLint 配置。

## 2.0.4

- 安全：音源脚本加载前扫描 constructor/process 逃逸 payload，命中即拒绝；SSRF 修复 IPv4-mapped IPv6（::ffff:）绕过与广播地址拦截。
- 修复：批量清理/去重删除记录后实时推送队列；下载目录扫描加短缓存；启动清理残留写探针文件；ffmpeg 元数据值去除控制字符；已设 AUTH_TOKEN 缺 SESSION_SECRET 时启动告警。

## 2.0.3

- 修复：批量换音质等接口报 Internal Server Error（readBody 漏传 event）；同时修复「已下载/多版本」列表标注、多版本入待确认、重新关联、删除孤儿文件等接口静默失效的问题。

## 2.0.2

- 新增：下载队列「下载失败」批量换音质，选中多个失败任务一键换音质重新下载。

## 2.0.1

- 修复：歌单一键解析下载会直接下载多版本（同名不同歌手）歌曲；现改为下载前预检，多版本自动加入「待确认」并附已有版本对比，由用户决定是否下载。

## 2.0.0

- 新增：下载列表「已下载」改为按磁盘真实文件数统计（记录按 file_path 去重且文件存在才计数），消除「软件已下载数」与「实际文件数」不一致。
- 新增：启动时与每 10 分钟自动校验「已完成/已存在」记录对应文件是否仍在（自愈标记），文件丢失时顶部提示并可一键清理；缺失文件在下载列表单独「文件缺失」筛选列出并归类原因。
- 新增：本地文件对账。扫描下载目录与记录双向对比，列出「匹配 / 缺失 / 疑似改名 / 孤儿文件 / 共享文件」五类差异；支持一键重新关联改名文件、删除孤儿文件、清理共享重复记录。
- 新增：下载前预检本地文件。搜索 / 排行榜 / 歌单广场列表直接标注「已下载」，已存在歌曲不再重复入队；批量入队自动跳过已存在并提示数量。
- 新增：音乐多版本。下载前预检区分「同名同歌手（已下载）/ 同名不同歌手（多版本）/ 无」，同名不同歌手不再误判为已存在；列表显示「多版本」按钮，弹窗列出本地已有版本供选择。
- 新增：多版本待确认持久化。多版本弹窗无人处理（关闭窗口 / 待会再说）时，歌曲保留在下载列表「待确认」区，可随时确认下载或跳过；批量入队遇到多版本自动加入待确认。

## 1.5.2

- 新增：音源手动排序（拖拽 + 上移/下移/置顶），下载取链按设置的顺序轮询音源。

## 1.5.1

- 修复：音源熔断误触发（「歌曲没有」不再计入熔断，检测音源可即时恢复）。
- 修复：取链失败错误提示简化为友好信息（识别常见错误码，截断原始 JSON）。
- 新增：下载取链失败时跨平台兜底，自动搜索其他平台匹配换源。

## 1.5.0

- 新增：歌单广场排序（最热 / 最新），优化三平台推荐歌单接口。
- 新增：歌单详情展示封面、描述、创建者、播放量等信息。
- 新增：试听链接缓存，减少重复取链。
- 新增：下载音质下拉选择菜单。
- 优化：歌单列表改为封面卡片展示。
- 新增：咪咕（mg）平台歌单广场支持。

## 1.4.3

- 修复：QQ 音乐歌单广场歌曲数量与歌单信息（改用 PlayListPlazaServer 接口，一次返回歌曲数 / 播放量 / 作者）。
- 新增：三平台歌单列表支持分页加载更多。

## 1.4.2

- 修复：QQ 音乐歌单广场数量现显示歌曲数量（xx 首）。
- 新增：歌单详情显示创建者、播放量、收藏量等信息。
- 变更：替换软件图标。

## 1.4.1

- 修复：QQ 音乐歌单广场歌单名乱码（接口默认 GBK，改为请求 UTF-8）。

## 1.4.0

- 新增：歌单广场功能，支持 wy / tx / kg 三平台歌单浏览（与链接导入共存），歌曲可直接入队下载。
- 变更：导航栏「队列」改名「下载列表」。

## 1.3.1

- 优化：排行榜数据缓存 10 分钟，避免频繁请求外部接口被风控；支持手动刷新榜单。

## 1.3.0

- 新增：排行榜功能，支持 wy / tx / kg 三平台榜单，歌曲可直接入队下载（下载阶段自动换源）。
- 变更：导航栏去掉底部导航，改为顶部导航；「歌单」改名「歌单下载」，新增「排行榜」入口。

## 1.2.6

- 新增：队列搜索功能，按歌名 / 歌手 / 文件名过滤任务，便于核实已下载内容。
- 新增：队列所有状态任务支持「手动匹配」与「删除」；进行中的任务手动匹配时先自动取消再换歌。

## 1.2.5

- 修复：队列「已存在」任务缺少「手动匹配」按钮，且出现重复「删除」按钮；手动匹配「已存在」任务不再误删下载目录中已有的文件。

## 1.2.4

- 变更：「已存在」检测改为优先完整命名匹配，未命中时按「歌名」宽松匹配（忽略歌手差异），歌名一致即视为已存在，避免重复下载。
- 变更：队列列表移除 200 条上限，显示全部任务。

## 1.2.3

- 变更：队列排序改为按更新时间倒序（最新在上）。任务入队/完成/失败时自动移到顶部，进度更新不改变顺序。

## 1.2.2

- 变更：下载并发上限由 5 提升到 20。

## 1.2.1

- 重构：整体 UI 视觉升级。深色主题配色、卡片、按钮、输入框、进度条、滚动条等细节全面精致化；顶部/底部导航采用毛玻璃与胶囊高亮，品牌 logo 改为渐变。

## 1.2.0

- 修复：设置页打开卡住一直转圈。飞牛 JS SDK 初始化与授权状态查询改为后台异步执行，不再阻塞设置页加载。
- 修复：批量下载时软件卡顿。下载进度上报改为节流（每 200ms 一次），避免高频同步写库与 SSE 推送阻塞事件循环。

## 1.1.3

- 修复：下载在「建立连接 / 等待响应头」阶段无超时保护，慢音源可能永久卡死；超时保护现覆盖整个下载过程（含握手），超时自动换源重试。
- 修复：手动匹配「下载完成」任务时先删旧文件、重新下载失败会导致原歌曲丢失；现改为先改名备份，新文件下载成功后再删除、失败自动恢复。
- 修复：手动匹配重新入队后仍残留原歌单归属信息（批次/歌单/匹配分数），现一并清空。
- 新增：手动匹配重新下载同样做「已存在」检测，目标歌曲已在下载目录时标记「已存在」，不再重复下载覆盖。
- 优化：下载超时错误码显式纳入自动重试判定；ffmpeg / ffprobe 探测增加超时收敛，避免进程异常时二次回调或永久挂起。

## 1.1.2

- 变更：下载文件默认命名规则改为「歌名 - 歌手」（`{title} - {artist}`）。
- 新增：队列「下载完成」任务支持手动匹配重新下载。已下载的歌曲可重新搜索匹配换歌后重下（删除旧文件重新入队）。

## 1.1.1

- 修复：下载卡在「100% 下载中」无法完成。下载流、ffprobe、ffmpeg 三个环节原先均无超时保护，文件损坏或网络挂载目录下会永久挂起；现增加下载断流 60s / 整体 60 分钟超时、ffprobe 30s 超时、ffmpeg 60s 超时，超时自动换源重试或跳过，任务不再卡死。
- 新增：队列失败/已取消任务支持「手动匹配」。可跨平台搜索并重选正确歌曲，替换任务信息后重新入队下载（保留原音质与歌词设置）。
- 新增：队列新增「已取消」筛选栏，支持批量重试与批量删除。

## 1.1.0

- 修复：歌单匹配不再自动覆盖原曲名。歌单列表始终显示原名，匹配候选在旁边展示；只有用户确认选择后才改用候选名下载。
- 修复：一键解析下载用歌单原名下载（不自动改名）；匹配存疑/低分（<0.7）不入队，标记失败供歌单页确认。
- 修复：歌单匹配遍历所有平台取全局最高分（原平台即使低分命中也会继续搜索其余平台，避免漏掉其他平台更匹配的结果）。

## 1.0.9

- 修复：歌单匹配误选歌手不符的版本。歌手改为硬性匹配门槛（无任何歌手交集即否决），支持多歌手拆分（周杰伦/费玉清 任一命中即可），杜绝「标题相同但歌手不同」的翻唱/合唱版本被自动下载。
- 新增：队列「已存在」检测。入队下载前检查下载目录是否已有同模板同名歌曲文件，存在则标记为「已存在」状态并停止下载；队列新增「已存在」筛选，支持强制重新下载或删除。
- 修复：「已存在」检测按请求音质匹配扩展名（请求 flac 只认 .flac，避免已有 mp3 误判 flac 已存在）。
- 修复：一键解析下载也校验匹配分数，低分候选不再自动入队（与预览需确认行为一致）。

## 1.0.8

- 修复：无损音质校验改为「实际格式必须是 FLAC」才算成功，音源返回 m4a/ape/ogg 等非 FLAC 格式时同样自动换源。
- 修复：换源记录坏源改用取链实际返回的音源，避免任务无 source_id 时换源失效、同源反复重试。

## 1.0.7

- 修复：下载遇 HTTP 403 等下载失败时自动切换其他音源重试，试完所有音源才判定失败并汇总提示。

## 1.0.6

- 修复：下载无损音质（flac / flac24bit）时，若音源返回 MP3（有损），自动切换其他音源继续尝试，直到下载到真正的无损；全部音源尝试后仍无无损才判定失败，并汇总提示各源失败原因。
- 修复：队列下载进度实时刷新（SSE 未拼接飞牛网关 baseURL 导致连不上，需手动刷新；已修复并增加断线自动重连）。

## 1.0.5

- 新增：歌单解析与一键下载支持跨平台搜索匹配。原平台无命中时自动在其余平台（kw/kg/tx）搜索，score≥0.4 自动入队并标记「跨平台 · 来源平台」；歌单页匹配列表显示跨平台徽标。
- 新增：队列下载中显示实时进度与已下载/总大小（MB），下载完成显示文件大小。

## 1.0.4

- 变更：移除「最高可用（自动降级）」音质选项；下载音质必须手动选择固定档位，默认 flac24bit。
- 试听保持最高可用自动降级，不影响播放。

## 1.0.3

- 新增：歌单导入下载默认预选设置中的音质、歌词开关与歌词写入方式。
- 新增：下载队列批量管理（全选、批量取消、批量重试、批量换源、批量删除，删除可选同时清理本地文件）。
- 修复：选择无损音质（flac / flac24bit）时，若音源实际返回 MP3，任务将标记失败并提示换源，不再把 MP3 冒充 FLAC 保存。

## 1.0.2

- 新增：下载队列按状态筛选（全部 / 下载中 / 下载失败 / 下载完成）。
- 新增：歌单一键解析下载（解析 + 匹配 + 入队一步完成）。
- 新增：音源支持从文件夹批量上传 .js 脚本。
- 移除：音源完整包导入 / 导出。

## 1.0.1

- 修复：飞牛网关下界面打不开（Git Bash 构建时 MSYS 路径转换导致应用 baseURL 损坏，现已在打包脚本中禁用）。

## 1.0.0

- 首个版本：网关访问、音源管理、多源搜索、下载队列、歌单导入、元数据与封面、目录一键授权。
